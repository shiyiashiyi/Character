# Character · Persona 工坊 —— 设计规划书（v2）

> 版本：v2.0
> 定位：学习项目，但核心链路（Persona 生成）按「可对外使用的私有工具」标准设计。
> 依据：对 v1 现状的复盘 + 行业主流做法（SillyTavern character card、`novel-character-persona-forge` 族 Claude skill、LLM persona-driven 框架）。

---

## 1. 项目定位与目标

### 1.1 定位

从「小说原文」自动生成「可复用的角色人设卡（角色卡）」，用于驱动大模型稳定扮演该角色。
技术栈：Vue 3 前端 + ASP.NET Core（.NET 8）后端 + SQL Server。

### 1.2 目标（v2 必须达成）

| 编号 | 目标 |
|---|---|
| G1 | 产物是「有意义的角色卡」：可直接驱动 LLM 稳定扮演，无需大量人工补全 |
| G2 | 人设的每一条断言都可回溯到原文证据（带章节/位置引用） |
| G3 | 具备**真实鉴权**（不再「假登录」） |
| G4 | 工程化到位：EF 迁移、测试、CI、限流、可观测 |

### 1.3 非目标（v2 不做）

- 不做多用户 SaaS 级能力、支付、移动端。
- 不追求 100% 自动化——保留「人工校对」入口，但**默认输出应已接近可用**（而不是半成品空模板）。

---

## 2. 现状复盘（v1 的主要问题）

| 维度 | 问题 | 影响 |
|---|---|---|
| 鉴权 | 后端无任何 `[Authorize]`/JWT，前端 localStorage 会话只是 UI 门槛 | `/forge` 可被任意调用 |
| 登录安全 | 登录接口无限速、无锁定 | 可被暴力破解 |
| 用户枚举 | `send-code` 对已注册邮箱返回 409 | 泄露邮箱是否注册 |
| 生成核心 | 规则模式用正则抽取，**不「理解」角色**；AI 模式只读草稿、看不到原文 | 产出的性格/动机/价值观为 `[待核对]` 空位 |
| 输入 | `workTitle`/`chapterRange` 只是标签，不参与抽取 | 用户以为在筛选，实际无效 |
| 语气 | 只有一条硬编码示例对话 | 无法靠 few-shot 锁定口吻 |
| 数据 | 建表靠 SQL 脚本、EF 无 migration；验证码表无清理 | schema 漂移、表膨胀 |
| 工程 | 无测试、无 CI、进度为前端定时器模拟 | 质量无保障、进度失真 |

---

## 3. 总体架构

```
┌─────────────────────────────────────────────────────────┐
│  前端 (Vue 3 + Vite)                                      │
│  LoginView / HomeView / PersonaForgeView / CardLibrary    │
│  token 会话 · 真实进度(SSE/轮询) · 角色卡预览/编辑/导出      │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTPS + Bearer Token (JWT)
┌──────────────────────────▼──────────────────────────────┐
│  API 层 (ASP.NET Core)                                    │
│  AuthController · PersonaController · CardsController      │
│  [Authorize] · 限流 · 异步任务                             │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│  领域服务层                                                │
│  AuthService · EmailVerificationService                   │
│  PersonaPipeline（多阶段生成流水线，核心）                  │
│  ├─ Preprocess 预处理/分块                                 │
│  ├─ Retrieve  检索/定位 (可选 RAG)                         │
│  ├─ Extract   证据抽取 (规则粗筛 + LLM 精提)               │
│  ├─ Synthesize 人格综合 (LLM 显式产出性格/动机/价值观)      │
│  ├─ Exemplify 语气样本 (LLM 生成多条示例对话)              │
│  └─ Assemble  组装输出 (SKILL.md + evidence + 角色卡)      │
└──────────────────────────┬──────────────────────────────┘
                           │ EF Core (migrations)
┌──────────────────────────▼──────────────────────────────┐
│  数据层 (SQL Server)                                       │
│  Users · EmailVerificationCodes · CharacterCards          │
│  GenerationJobs · 定期清理任务                              │
└─────────────────────────────────────────────────────────┘
```

---

## 4. 核心设计：Persona 生成流水线（重设计）

### 4.1 设计原则（对齐行业共识）

| 原则 | 内容 | 对应 v1 差距 |
|---|---|---|
| P1 结构化角色卡 | 用明确的字段表示角色（非散文） | 半对齐（有 Markdown，缺人格字段） |
| P2 LLM 直读原文 | 让模型通读原文综合，必要时 RAG | ❌ 规则模式正则、AI 模式只读草稿 |
| P3 证据可追溯 | 每条断言带原文引用（章节/位置） | ✅ 已具备，保留 |
| P4 人格显式产出 | 性格/动机/价值观/关系网显式生成 | ❌ 空 `[待核对]` |
| P5 few-shot 定语气 | 多条示例对话锁定口吻 | ❌ 单条硬编码 |
| P6 约束带优先级 | 冲突时按优先级裁决，非平铺 | ❌ 平铺 MUST/MUST NOT |
| P7 异步 + 真实进度 | 生成任务异步化，进度真实上报 | ❌ 前端定时器模拟 |

### 4.2 角色卡数据模型（输出契约，对齐 SillyTavern V3）

生成产物的**权威格式**是一个结构化角色卡（JSON），Markdown 是它的可读呈现：

```jsonc
{
  "schema": "character-card-v1",
  "name": "persona-weixin",
  "characterName": "隗辛",
  "work": { "title": "《穿进赛博游戏后干掉BOSS成功上位》", "chapters": "1-30" },
  "identity": {
    "aliases": ["隗辛", "她", "玩家"],
    "role": "赛博游戏玩家",
    "appearance": "...",
    "summary": "一句话身份（由模型综合，非取第一条旁白）"
  },
  "personality": [
    { "trait": "冷静", "intensity": 0.8, "evidence": "第3章·「……」" },
    { "trait": "多疑", "intensity": 0.6, "evidence": "第7章·「……」" }
  ],
  "motivation": { "goals": ["活下去"], "values": ["生存优先"], "bottomLines": ["不背叛队友"] },
  "backstory": [ { "event": "...", "effect": "使其更谨慎", "evidence": "第N章" } ],
  "relationships": [ { "person": "某角色", "relation": "队友", "evidence": "第N章" } ],
  "speechStyle": {
    "catchphrases": ["好耶", "靠"],
    "sentenceLength": "短句",
    "register": "现代口语 / 半文白",
    "notes": "遇到危险先分析局势"
  },
  "constraints": {
    "priorityOrder": ["安全与隐私", "事实与真实性", "用户意图", "核心任务", "人设一致性", "语言风格"],
    "must": ["不跳出角色", "无证据标注推测"],
    "mustNot": ["不编造重大情节", "不用网络流行语"]
  },
  "examples": [
    { "user": "……", "char": "……" },
    { "user": "……", "char": "……" }
  ]
}
```

**要点**：`personality`/`motivation`/`backstory` 的每一项都挂 `evidence`（引用），满足 G2；`examples` 是多条、由模型模仿证据生成的，满足 P5。

### 4.3 流水线阶段

```
原文
  │
  ▼
[Stage 0 预处理]  编码识别(UTF-8/GB18030) → 分章 → 按上下文窗口分块(带重叠)
  │
  ▼
[Stage 1 检索/定位]  按角色名 + 别名（含昵称/代词）检索相关块
  │                  短文本直接全量；长文本走向量检索(RAG)
  ▼
[Stage 2 证据抽取]  规则做「粗筛索引」+ LLM 做「精提取与去噪」
  │                  每条原话/旁白带 {章节, 位置} 引用，并做说话人归属校验
  ▼
[Stage 3 人格综合]  LLM 依据证据显式产出 identity/personality/motivation/
  │                backstory/relationships，每条断言回挂证据
  ▼
[Stage 4 语气样本]  LLM 依据 speechStyle + 证据台词，生成 N 条「像本人」的示例对话
  │
  ▼
[Stage 5 组装输出]  角色卡 JSON + SKILL.md + source-evidence.md + 置信度标注
```

**关键点**：

- **LLM 是主路径，规则降级为「证据索引器」**：正则只负责粗筛「哪些片段可能相关」，真正的理解（归属校验、人格综合、语气）交给 LLM 读原文完成。这修正了 v1「AI 只能读草稿、被规则天花板卡住」的结构性缺陷。
- **别名扩展**：Stage 1 前先做别名/昵称识别（可用 LLM 或词典），解决中文小说代词/外号导致的漏抽。
- **说话人归属校验**：Stage 2 对每条候选台词，让 LLM 判断「这句话是否真是目标角色说的」，消除 v1 兜底正则的误归属。

### 4.4 双模式重新定位

| 模式 | 新定位 | 说明 |
|---|---|---|
| `rule`（轻量） | 仅 Stage 0→2，产出「证据库」，不产人格 | 定位为「离线/无 Key 时的证据索引」，明确标注「不含人格综合」 |
| `ai`（主路径） | Stage 0→5 全流程 | 默认模式；失败时**回退返回 rule 证据库**，而非 503 丢弃 |

### 4.5 输出文件结构

```
输出目录 / <slug> /
├── character-card.json      # 权威结构化角色卡（对齐 SillyTavern V3）
├── SKILL.md                 # 可读人设（YAML frontmatter + 人格/语气/约束/示例）
└── references/
    └── source-evidence.md   # 带章节引用的原文证据库
```

---

## 5. 认证与会话设计（补真实鉴权）

- **令牌**：登录/注册成功返回 JWT（access，短时效）+ refresh token（HttpOnly Cookie，可选）。
- **保护**：`PersonaController`、`CardsController` 加 `[Authorize]`；前端 `Authorization: Bearer <token>`。
- **前端会话**：`useSession` 存 token（而非裸用户对象），路由守卫校验 token 存在且未过期，失效即跳登录。
- **登录限速**：per-IP + per-account 失败计数，超阈值锁定/退避（ASP.NET Rate Limiting middleware）。
- **防用户枚举**：`send-code` 对「已注册」与「未注册」邮箱返回**相同文案**，重复判断移到 `register`。
- **验证码**：保留 v1 的哈希存储 + 常量时间比较 + 冷却 + 次数上限（这部分已达标）。

---

## 6. 数据模型设计

| 表 | 用途 | 变更 |
|---|---|---|
| `Users` | 账户 | 保留；可加 `RefreshTokenHash` |
| `EmailVerificationCodes` | 验证码 | 保留；加定时清理（过期记录定期删除） |
| `CharacterCards` | 生成的角色卡持久化 | 新增（userId、slug、cardJson、skillMd、evidenceMd、状态、时间戳） |
| `GenerationJobs` | 异步生成任务 | 新增（jobId、userId、状态、进度、错误） |

- 全部表改由 **EF Core migrations** 管理，废弃「手写 SQL 脚本 + EF 映射」的双轨，消除 schema 漂移。

---

## 7. API 设计

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/api/auth/send-code` | 无 | 限流；统一文案防枚举 |
| POST | `/api/auth/register` | 无 | 校验验证码 |
| POST | `/api/auth/login` | 无 | 限速；返回 JWT + 用户 |
| POST | `/api/auth/refresh` | refresh | 刷新令牌 |
| POST | `/api/persona/forge` | ✅ | 提交生成任务，返回 `jobId` |
| GET | `/api/persona/forge/{jobId}` | ✅ | 轮询进度/结果 |
| GET | `/api/persona/forge/{jobId}/events` | ✅ | SSE 实时进度 |
| GET/PUT/DELETE | `/api/cards/...` | ✅ | 角色卡 CRUD |
| GET | `/api/health/db` | 无 | 联调诊断 |

- 生成由「同步一次性返回」改为「**异步任务 + 进度上报**」，进度来自后端真实阶段（P7），前端不再用定时器假装。

---

## 8. 前端设计

- **会话**：token 存于内存/localStorage，请求头自动附带；401 统一跳登录。
- **真实进度**：订阅 `events`（SSE）或轮询 `GET .../{jobId}`，按后端上报的阶段渲染（当前 `startStageTicker` 定时器移除）。
- **角色卡编辑/预览**：生成后进入编辑器，可改 `personality/motivation/constraints`，保存到 `CharacterCards`。
- **导出**：`.json`（角色卡）、`SKILL.md`、`source-evidence.md`，以及 PNG 角色卡（可选，v2 后置）。
- **HomeView**：把「角色对话 / Skill 库 / 账户设置」中的 Skill 库从「开发中」接上 `CharacterCards`。

---

## 9. 非功能性需求

| 类别 | 要求 |
|---|---|
| 安全 | JWT 鉴权、登录限速、防枚举、AI 调用加超时/重试、防提示注入（原文进 prompt 时加隔离标记） |
| 性能 | 上传大小限制保留；长文本走分块/RAG；AI 调用设 `HttpClient.Timeout` + 退避 |
| 可观测 | 结构化日志（阶段、耗时、token 用量）、生成任务状态可查 |
| 测试 | 服务层单测（抽取/人格综合/权限）、流水线 golden 用例、API 集成测试 |
| CI | GitHub Actions：build + test + lint（`.github/workflows` 补齐） |

---

## 10. 里程碑 Roadmap

| 里程碑 | 内容 | 对应目标 |
|---|---|---|
| M1 认证加固 | JWT + `[Authorize]` + 登录限速 + 防枚举 | G3 |
| M2 流水线重写 | Stage 0→5：LLM 直读原文 + 人格综合 + 多条示例对话 | G1、G2 |
| M3 数据层 | EF migrations + CharacterCards + 验证码清理 | G4 |
| M4 体验 | 异步任务 + SSE 真实进度 + 角色卡编辑器/导出 | G1、P7 |
| M5 工程化 | 测试 + CI + 可观测 + 超时/重试 | G4 |

---

## 11. 验收标准（「有意义 skill」的判据）

1. **人设一致性**：仅凭生成的 `character-card.json`，LLM 在 ≥20 轮对话中保持在角色内（性格/动机/口吻不漂移）。
2. **可追溯性**：`personality/motivation/backstory/relationships` 的每一项断言都能在 `source-evidence.md` 中找到对应原文（抽查命中率 ≥90%）。
3. **无幻觉**：抽查生成内容，无「原文不存在」的重大情节/关系/身份设定。
4. **鉴权有效**：未携带有效 token 调用 `/api/persona/forge` 返回 401。
5. **进度真实**：前端进度与后端任务阶段一致（非定时器模拟）。
